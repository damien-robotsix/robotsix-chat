name: Deps Bump

on:
  schedule:
    - cron: "0 6 * * 1"   # every Monday 06:00 UTC
  workflow_dispatch:

permissions:
  contents: read

jobs:
  deps-bump:
    uses: damien-robotsix/robotsix-mill/.github/workflows/deps-bump.yml@main
    permissions:
      contents: write
      pull-requests: write
    secrets: inherit
