name: Baseline Check

# Repo-baseline conformance gate (robotsix-standards): AGENT.md/README
# standards links, MIT LICENSE, dependabot ecosystem coverage, towncrier
# wiring, and docs/modules.yaml drift (via robotsix-modules).
on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

concurrency:
  group: baseline-check-${{ github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

jobs:
  baseline:
    name: Baseline (shared)
    uses: damien-robotsix/robotsix-github-workflows/.github/workflows/baseline-check.yml@a6378accaf26c75b12fac324c3056255647c107b  # main
    with:
      has-docker: true
      has-board: true
      python-version: "3.14"
