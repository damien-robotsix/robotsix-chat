name: Weekly container vulnerability rescan
on:
  schedule:
    - cron: '0 6 * * 1'
  workflow_dispatch:
permissions:
  contents: read  # baseline: checkout the repo

concurrency:
  group: scan-container-${{ github.ref }}
  cancel-in-progress: false

jobs:
  rescan:
    name: Weekly Trivy rescan (shared)
    permissions:
      security-events: write  # upload Trivy SARIF to Code Scanning
    uses: damien-robotsix/robotsix-github-workflows/.github/workflows/scan-container.yml@43309967ea8011400212a8995d33ca900ee2afed  # main
