name: Weekly container vulnerability rescan
on:
  schedule:
    - cron: '0 6 * * 1'
  workflow_dispatch:
permissions:
  contents: read  # baseline: checkout the repo

concurrency:
  group: scan-container-${{ github.ref }}
  cancel-in-progress: false

jobs:
  rescan:
    name: Weekly Trivy rescan (shared)
    permissions:
      security-events: write  # upload Trivy SARIF to Code Scanning
    uses: damien-robotsix/robotsix-github-workflows/.github/workflows/scan-container.yml@0234f4b82365d776fc021c774dc104c5e7042c29  # main
