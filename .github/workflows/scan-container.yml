name: Weekly container vulnerability rescan
on:
  schedule:
    - cron: '0 6 * * 1'
  workflow_dispatch:
permissions:
  contents: read  # baseline: checkout the repo

concurrency:
  group: scan-container-${{ github.ref }}
  cancel-in-progress: false

jobs:
  rescan:
    name: Weekly Trivy rescan (shared)
    permissions:
      security-events: write  # upload Trivy SARIF to Code Scanning
    uses: damien-robotsix/robotsix-github-workflows/.github/workflows/scan-container.yml@44f210c7429969be62eb8cc4f53bfbd3b744da16  # main
