ci_fix: out-of-scope CI failure — Python CI (shared) / Tests — test_approve_wrong_state_returns_409 in tests/chat/server/test_autonomous_endpoints.py (test needs to account for single-session invariant — either create a session with a different owner or reset the runner state before calling create_session), or src/robotsix_chat/autonomous/runner.py (create_session may need an override parameter for tests)
