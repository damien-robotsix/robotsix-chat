Clarify how the chat agent writes its own configuration: the `set_component_config` tool docstring now states it configures other component agents only, and the `robotsix-chat-self` chat skill documents the agent's own `PUT /config` write path (with version history and rollback) while warning against routing self-config writes through the deploy plane's template-derived endpoints.
