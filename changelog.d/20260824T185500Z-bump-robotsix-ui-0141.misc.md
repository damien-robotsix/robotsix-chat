Bump the vendored robotsix-ui build to v0.1.41, whose config client refuses a `GET /config` response that carries no `config` document instead of rendering the form from schema defaults — the client-side half of the settings-panel config wipe.
