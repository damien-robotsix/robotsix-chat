Split the oversized `subsessions/worker.py` module by extracting the paused/queued/event-driven wait-loop machinery into a new `subsessions/subsession_waits.py` module, and split the large `test_direct_repo.py` and `test_worker.py` test modules into focused `test_ci_workflow.py` and `test_worker_wait_for_event.py` files. No behaviour change.
