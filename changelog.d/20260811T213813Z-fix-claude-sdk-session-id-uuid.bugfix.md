Fix chat turns failing with "The assistant hit an internal error and couldn't complete the response." The Claude SDK session-cache injection added in #1327 forwarded the chat session id (`uuid4().hex`, no dashes) to the CLI as `--session-id`, which rejects anything that is not a canonical UUID. Session ids are now mapped through a deterministic `uuid5`, so the CLI accepts them and still reuses its session cache across turns.
