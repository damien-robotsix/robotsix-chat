Bumped `pypdf` to 6.15.0, closing GHSA-fp3f-mc75-235c and GHSA-fwg2-594c-jp42
(both resource exhaustion). The advisories landed against the pinned version, so
CI started failing on their own — the same fresh advisory that reddened mill.
