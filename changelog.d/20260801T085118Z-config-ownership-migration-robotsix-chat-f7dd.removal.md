Removed lifecycle config-store tools (`get_lifecycle_service_config`, `update_lifecycle_service_config`, `watch_service_redeploy`) and their client methods from the deploy-lifecycle integration. Components now own their configuration internally via `/config` endpoints; the central-deploy config-store API is no longer used at runtime. Deploy-boundary config (volume mounts, port mappings, image refs, boot-time secrets) is unchanged.
