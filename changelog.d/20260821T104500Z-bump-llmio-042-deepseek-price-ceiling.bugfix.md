Bump the `robotsix-llmio` pin past v0.4.2 (`e5509fd1`). The pinned 0.4.0 build carried the stale DeepSeek `max_price` ceiling of $0.70/$1.40 per 1M tokens, which no `deepseek/deepseek-v4-pro` endpoint reliably satisfies after DeepSeek's August price rise — capable-tier calls returned `404 No endpoints found that satisfy the max price for this request`. The new pin carries the re-measured $0.90/$2.40 capable ceiling, the $0.25/$0.70 cheap ceiling that re-admits DeepSeek's own endpoint, and a `provider.ignore` list excluding the cache-read outliers (DigitalOcean, CoreWeave) that `max_price` cannot see.
