A degraded memory backend is now visible in the chat UI: a banner reports when cognee is not working and that replies are running without recall. Detection no longer relies solely on a list of known fault signatures — an unrecognised error that repeats on every recall now marks the backend degraded, so a novel failure mode surfaces on `GET /health` and in the UI instead of failing silently.
