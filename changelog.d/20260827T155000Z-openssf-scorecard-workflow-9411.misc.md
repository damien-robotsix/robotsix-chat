Add an OpenSSF Scorecard workflow (weekly plus on pushes to `main`) that publishes results and uploads
SARIF to Code Scanning, matching the existing robotsix-github-auth setup, and surface the score as a
README badge.
