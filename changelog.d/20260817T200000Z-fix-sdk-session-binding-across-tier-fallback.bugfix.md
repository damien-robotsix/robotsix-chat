Fix every new chat failing while the configured model tier is out of credits. The SDK session id is derived deterministically from the chat session, so a tier fallback re-asked the CLI to create the id the primary attempt had already created ("Session ID <uuid> is already in use") — and the fallback path had no self-heal, so the turn was lost. Binding now comes from shared per-turn state instead of the message history, and both paths flip once when the CLI disagrees.
