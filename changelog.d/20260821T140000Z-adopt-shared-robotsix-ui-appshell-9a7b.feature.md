Adopt the shared `@robotsix/ui` AppShell (v0.1.40) as the persistent top-of-page chrome for the browser chat UI — mounts the fleet chrome with brand, cross-component nav links, and a Settings entry, and moves the per-component header controls (chats, subsessions, connection dot) into the AppShell's right slot. Also adopts the shared ConfigPanel (`mountConfigPanel`) for the settings side panel. This completes the fleet UI-unification rollout (file-hub, auto-mail, cost-monitor, invest, central-deploy, and board all adopted previously).