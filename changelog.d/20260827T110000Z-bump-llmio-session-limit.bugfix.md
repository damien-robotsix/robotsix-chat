Bump robotsix-llmio to f369b361, which classifies the Claude CLI's rolling session limit ("You've hit your session limit · resets HH:MMam") as usage exhaustion. It previously matched no signature and fell through to the degenerate-success frame the CLI wraps it in, which is treated as transient — so every call burned all three retries against the same limited tier and surfaced an opaque internal error, while the fallback model (gated on `ClaudeSDKUsageExhaustedError`) was never tried. Also picks up llmio #603, raising the level-2 output cap to 65536 so xhigh reasoning no longer consumes the whole budget before any content token.
