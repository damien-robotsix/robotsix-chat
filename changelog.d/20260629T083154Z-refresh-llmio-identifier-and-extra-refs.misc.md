Refresh stale `robotsix-llmio` references in comments/docs: the provider-model identifier
`openrouter[deepseek]-deepseek/…` is now `openrouter-deepseek/…` (llmio dropped the
bracketed-qualifier syntax), and the remaining `robotsix-llmio[openrouter-deepseek]` extra
references are updated to `[openrouter]`. Comments/docs only — no behavior change.
