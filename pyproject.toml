[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "robotsix-chat"
version = "0.1.0"
description = "Browser + SSE chat server exposing an LLM agent to human users"
readme = "README.md"
requires-python = ">=3.14"
license = { text = "MIT" }
authors = [{ name = "Damien Robotsix", email = "damien.robotsix@gmail.com" }]
dependencies = [
    "pydantic",
    "python-dotenv",
    "uvicorn",
    "starlette",
    # Shared robotsix-stack libraries, consumed via git (see [tool.uv.sources]):
    #   - robotsix-yaml-config: the YAML config-cascade.
    #   - robotsix-llmio: provider-agnostic LLM I/O; the chat picks a
    #     `transport` + `model_level` and llmio resolves the backend. The base
    #     package is enough to import the config factory; the actual backend
    #     deps come from the per-transport extras below.
    "robotsix-yaml-config",
    "robotsix-llmio",
    "pyyaml>=6",
]
classifiers = [
    "Development Status :: 2 - Pre-Alpha",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.14",
    "Typing :: Typed",
]

[project.optional-dependencies]
# Per-transport backends, obtained THROUGH robotsix-llmio's own extras so the
# stack owns those deps in one place. Select via `llmio.transport` in config.
#
# claude-sdk: Claude Agent SDK — subscription auth via a logged-in `claude` CLI
# (no API key), needs Node.js + the `claude` CLI at runtime.
claude-sdk = ["robotsix-llmio[claude_sdk]"]
# openrouter: OpenRouter (deepseek) — needs `llmio.api_key`.
openrouter = ["robotsix-llmio[openrouter-deepseek]"]

[project.scripts]
robotsix-chat = "robotsix_chat.chat.server:run_server_from_config"

[dependency-groups]
dev = [
    "ruff",
    "mypy",
    "pytest",
    "pytest-asyncio",
    "pytest-cov",
    "pre-commit",
    "httpx",
]

[tool.hatch.build.targets.wheel]
packages = ["src/robotsix_chat"]

[tool.uv.sources]
robotsix-yaml-config = { git = "https://github.com/damien-robotsix/robotsix-yaml-config.git", rev = "main" }
robotsix-llmio = { git = "https://github.com/damien-robotsix/robotsix-llmio.git", rev = "main" }

[tool.ruff]
target-version = "py314"
line-length = 88

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM"]

[tool.mypy]
python_version = "3.14"
strict = true

[tool.pytest.ini_options]
strict = true
testpaths = ["tests"]
filterwarnings = ["error"]

[tool.coverage.run]
source = ["robotsix_chat"]
branch = true
relative_files = true

[tool.coverage.report]
exclude_lines = ["pragma: no cover", "if TYPE_CHECKING:"]
