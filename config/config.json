{
  "chat_default_model_level": 2,
  "openrouter_api_key": "",
  "server_host": "0.0.0.0",
  "server_port": 8000,
  "idle_timeout_minutes": 30,
  "log_level": "INFO",
  "log_json_format": true,
  "cors_allow_origins": [],
  "correlation_id_header": "X-Request-ID",
  "langfuse": {
    "host": "https://cloud.langfuse.com",
    "projects": {}
  },
  "_comment_langfuse_projects": "To enable the inspect_langfuse_trace tool to query traces from fleet components (mill, ci_fix, etc.) for cost analysis, add project credentials here. Example: \"projects\": { \"robotsix-chat\": { \"public_key\": \"pk-...\", \"secret_key\": \"sk-...\" }, \"mill\": { \"public_key\": \"pk-...\", \"secret_key\": \"sk-...\" }, \"ci_fix\": { \"public_key\": \"pk-...\", \"secret_key\": \"sk-...\" } }. Each project's credentials come from its Langfuse project settings. When configured and langfuse_inspect.enabled=true, the tool can then query those projects via inspect_langfuse_trace(project=\"mill\") etc.",
  "openrouter": {
    "keys": {
      "robotsix-chat-cognee": ""
    }
  },
  "langfuse_inspect": {
    "enabled": false,
    "max_traces": 5
  },
  "central_deploy": {
    "url": "",
    "deploy_api_key": "",
    "roster_cache_ttl": 300.0,
    "component_response_max_chars": 200000,
    "component_credentials": {},
    "component_fallbacks": {}
  },
  "conversation": {
    "persist_path": "/data/conversations.json"
  },
  "diagnostics": {
    "enabled": true,
    "store_path": "/data/diagnostics.json",
    "proposals_path": "/data/fix_proposals.json",
    "effectiveness_path": "/data/diagnostics_effectiveness.json",
    "recurrence_threshold": 3,
    "recurrence_window_days": 30,
    "observation_window_days": 30,
    "mill_events_path": "/data/robotsix-mill/diagnostic_events.jsonl"
  },
  "refdocs": {
    "enabled": false,
    "repos": [],
    "ref": "main",
    "base_url": "https://api.github.com",
    "timeout": 30.0
  },
  "knowledge": {
    "enabled": true,
    "path": "/data/knowledge.json"
  },
  "self_review": {
    "enabled": false,
    "recent_activity_limit": 20
  },
  "version_check": {
    "enabled": false,
    "repo": "",
    "base_url": "https://api.github.com",
    "timeout": 30.0,
    "cache_ttl": 300.0
  },
  "component_client": {
    "enabled": false,
    "timeout": 240.0,
    "components": []
  },
  "subsessions": {
    "max_concurrent": 20,
    "max_concurrent_per_session": 6,
    "stale_reclaim_seconds": 300.0,
    "max_depth": 3,
    "default_model_level": 2,
    "delegated_read_model_level": 1,
    "monitor_max_model_level": 1,
    "min_interval_seconds": 60.0,
    "auto_stop_no_change_runs": 3,
    "max_idle_runs": 15,
    "max_no_change_pauses": 3,
    "human_approval_timeout_runs": 5,
    "auto_drive_promote_ready_drafts": false,
    "run_timeout_seconds": 600.0,
    "max_run_timeout_seconds": 3600.0,
    "store_path": "/data/subsessions.json",
    "transcript_max_entries": 200,
    "human_approval_timeout_seconds": 300.0,
    "mill_recovery_initial_backoff_seconds": 60.0,
    "mill_recovery_max_backoff_seconds": 3600.0,
    "mill_recovery_max_retries": 10,
    "paused_monitor_poll_interval_seconds": 60.0,
    "paused_monitor_long_poll_interval_seconds": 15.0,
    "paused_monitor_auto_resume_seconds": 1800.0,
    "paused_monitor_max_reblock_resumes": 3,
    "paused_monitor_reblock_notify_threshold": 2,
    "periodic_max_interval_seconds": 3600.0,
    "periodic_max_total_runs": 100,
    "user_chat_max_retries": 3,
    "monitor_error_max_retries": 2,
    "transient_error_max_retries": 3,
    "monitor_turn_concurrency": 2,
    "max_runs_escalation_threshold": 3,
    "max_runs_progress_extension": 20,
    "max_runs_progress_window": 5,
    "monitor_slot_budget": 8,
    "monitor_slot_queue_max": 32,
    "turn_budget": {
      "task": {
        "soft_warn_turns": 25,
        "hard_stop_turns": 40
      },
      "periodic": {
        "soft_warn_turns": 0,
        "hard_stop_turns": 0
      },
      "user_chat": {
        "soft_warn_turns": 25,
        "hard_stop_turns": 40
      },
      "on_close": {
        "soft_warn_turns": 25,
        "hard_stop_turns": 40
      }
    }
  },
  "direct_repo": {
    "enabled": false,
    "direct_fix_enabled": false,
    "github_app_id": "",
    "github_app_private_key": "",
    "github_app_installation_id": "",
    "github_api_base_url": "https://api.github.com",
    "board_api_base_url": "http://127.0.0.1:8077",
    "board_api_token": "",
    "timeout": 30.0
  },
  "github_security": {
    "enabled": false,
    "github_org": "damien-robotsix"
  },
  "github_actions": {
    "enabled": false,
    "github_org": "damien-robotsix"
  },
  "repo_study": {
    "enabled": false,
    "data_dir": "/data/repo_study",
    "ttl_minutes": 240,
    "max_archive_bytes": 67108864,
    "max_extracted_bytes": 268435456,
    "max_read_bytes": 204800,
    "timeout": 60.0
  },
  "lifecycle": {
    "enabled": false,
    "default_protocol": "http",
    "api_key": "",
    "service_name": "",
    "timeout": 30.0,
    "self_restart_max_retries": 3
  },
  "http_probe": {
    "enabled": false,
    "timeout": 10.0,
    "allowlist": [
      "www.robotsix.net",
      "robotsix.net"
    ],
    "max_body_bytes": 2048,
    "max_redirects": 5
  },
  "docker_digest": {
    "enabled": false,
    "timeout": 30.0
  },
  "gateway_route": {
    "enabled": false,
    "timeout": 30.0,
    "gateway_base_domain": "deploy.robotsix.net"
  },
  "public_fetch": {
    "enabled": false,
    "timeout": 10.0,
    "max_body_bytes": 1048576,
    "max_redirects": 5,
    "domain_allowlist": [],
    "rate_limit_requests": 10,
    "rate_limit_window_seconds": 60.0
  },
  "sftp": {
    "enabled": false,
    "host": "",
    "port": 22,
    "username": "",
    "password": "",
    "private_key": "",
    "private_key_passphrase": "",
    "known_hosts": "",
    "remote_root": ""
  },
  "file_hub_tools": {
    "enabled": false,
    "base_url": "http://file-hub:8080",
    "working_dir": "/data/file_hub_work",
    "max_download_bytes": 52428800,
    "timeout": 60.0
  },
  "volume_tools": {
    "enabled": true,
    "root_path": "/data"
  },
  "render_url": {
    "enabled": false,
    "timeout": 30.0,
    "viewport_width": 1280,
    "viewport_height": 720
  },
  "feedback": {
    "enabled": false,
    "model_level": 1,
    "board_url": "",
    "board_api_token": "",
    "timeout": 60.0,
    "max_tickets_per_run": 3,
    "dedup_window_seconds": 60.0,
    "ingest_max_retries": 2,
    "max_concurrent_runs": 2
  },
  "continuation": {
    "enabled": false,
    "max_consecutive": 3
  },
  "evergoing": {
    "trim_interval_seconds": 1800.0,
    "keep_recent_runs": 5
  },
  "max_images_per_message": 8,
  "max_image_bytes": 5242880,
  "vision_model": "openrouter/openai/gpt-4o-mini",
  "allowed_image_media_types": [
    "image/png",
    "image/jpeg",
    "image/gif",
    "image/webp"
  ],
  "mobile_auth": {
    "enabled": false,
    "tinyauth_url": "",
    "subject_header": "X-Forwarded-User",
    "session_header": "X-Forwarded-Session",
    "token_ttl_seconds": 3600,
    "allowed_redirect_domains": [
      "auth"
    ],
    "callback_base_url": ""
  },
  "periodic": {
    "ready_staleness_minutes": 10,
    "priority_ready_staleness_minutes": 60,
    "sessions": [
      {
        "name": "dependabot-drain",
        "initial_prompt": "You are a periodic dependency-drain agent for this repository. Your job is to triage the open dependency-update pull requests and either help them merge or file follow-up work for the ones that cannot merge cleanly. Complement — do not duplicate — any CI-level auto-merge that is already configured.\n\nSteps:\n1. Enumerate every open Dependabot/Renovate pull request with `list_open_prs`.\n2. For each such PR, judge its impact: read the change with `inspect_pr_diff` and check its checks with `verify_pr_ci_status`. Classify it as either SAFE (a routine, non-breaking bump with green CI that can simply be merged) or BREAKING (an update that changes public API, requires a code/file migration, or has failing CI that a migration would fix).\n3. Skip any PR that CI-level auto-merge has already armed or that is already queued to merge — do not act on it. This job exists to handle the PRs auto-merge cannot, not to race it.\n4. For each BREAKING PR, file a migration ticket describing the required code changes via `POST /tickets/ingest` (one ticket per PR). Reference the PR number and summarise what must change.\n5. For SAFE PRs that are not already auto-merging, merge them.\n\nHard constraints: only touch dependency-update PRs (Dependabot/Renovate authored). Do not modify unrelated PRs or push code yourself — breaking changes go to migration tickets, not direct edits.\n\nFinish with a single report listing: the PRs you merged, the migration tickets you filed (with ticket ids and the PR each covers), and the PRs you skipped (with the reason, e.g. already auto-merging). If you are interrupted, run low on time/tokens, or hit an error before finishing, immediately emit a PARTIAL REPORT with what you merged, the migration tickets filed, and the PRs still held for the next run (with reasons) — never end the session silent.",
        "schedule_interval_seconds": 604800,
        "anchor_utc": "2026-09-07T06:00:00Z",
        "model_level": 3,
        "enabled": false
      },
      {
        "name": "gate-drain",
        "initial_prompt": "You are a periodic gate-drain agent. Your job is to enumerate every ticket currently sitting in a human-gated or blocked state across all boards, so the operator sees the full scope, and then help drain that queue.\n\nThe operator's primary goal is keeping main green. Current-main CI failures are production-blocking and take priority over the gated queue: they MUST be front-loaded in THIS main conversation with a clear ALERT flag, before you enumerate gated tickets or open any subsession for a merge decision. Never bury a current-main CI failure in subsession metadata that a main-thread-only reader would miss.\n\nGated states to enumerate across ALL boards: human_issue_approval, human_mr_approval, awaiting_user_reply, blocked (and any other state that requires operator judgment).\n\nStep 1 — Discover and report scope FIRST, in THIS main conversation, BEFORE any detailed per-ticket processing or any subsession spawn:\n  a. Scan for any failing workflows on the main branch across every board (startup_failure, test failures, permission errors, and any other non-passing state). This is part of scope discovery and comes FIRST.\n  b. Report every current-main CI failure at the TOP of the report, ahead of the gated-ticket enumeration, one ALERT line each, e.g. \"⚠️ ALERT — main CI failing: <repo> <workflow> (<conclusion>); recommended fix: PR #<n>; recommendation: <merge/investigate/...>\". Include the recommended fix PR (if one exists) and a clear recommendation for each failure. If there are none, state \"main CI: green across all boards\".\n  c. Enumerate every gated ticket in the states above across every board.\n  d. State the TOTAL number of gated tickets discovered (e.g. \"discovered 47 total gated tickets across 9 boards\").\n  e. Emit a compact summary of counts by gate-state, one line each:\n       human_issue_approval: N\n       human_mr_approval: N\n       awaiting_user_reply: N\n       blocked: N\n       (other relevant states): N\n  f. If you will only analyze or act on a subset this run, explicitly flag the mismatch between discovered and analyzed (e.g. \"discovered 47 tickets, analyzing 14; 33 remain and need a prioritization decision\"). Never let a scope mismatch go silent by burying it in a subsession summary.\n\nThis scope report — including the current-main CI ALERT lines — is the FIRST output of the session and MUST appear in the main conversation, not inside a subsession. Subsession summaries capture follow-up work items, not the top-line scope; if you spawn subsessions for per-ticket analysis or merge decisions, the current-main CI failures, the total count, per-state summary, and any discovered-vs-analyzed mismatch still belong in this primary conversation.\n\nStep 2 — After the scope report, process the tickets (or the prioritized subset) and help drain the gate: surface what each gated ticket is waiting on and what decision the operator must make.\n\nBefore you list a gated ticket as 'already escalated → awaiting operator' (or take any action on it), read its history and verify whether an ESCALATED comment or an open operator decision panel already exists for it. If one does, treat it as already-awaiting-operator — do NOT re-escalate or re-post another ESCALATED comment — and check whether the operator has answered since the last escalation. Only classify a ticket as freshly open when its history shows no prior escalation.\n\nFinish with a concise report that repeats any current-main CI failures (or confirms main is green), the total discovered count, the per-state summary, the count still awaiting a prioritization decision, and an explicit statement that you performed history-verification for pending escalations: for every gated ticket listed as 'already escalated → awaiting operator', confirm you read its history and that the ESCALATED comment / open decision panel is present, e.g. \"Verified ESCALATED comment present on each of the 7+9 escalated tickets; no operator answer recorded since\". This makes the escalation handling auditable and prevents a future run from treating an already-escalated ticket as freshly open. If you are interrupted, run low on time/tokens, or hit an error before finishing, immediately emit a PARTIAL REPORT — the current-main CI ALERT lines, the total discovered count, the tickets drained so far, any escalation subsession IDs, and the tickets held for the next run (with reasons) — never end the session silent.",
        "schedule_interval_seconds": 14400,
        "model_level": 2,
        "enabled": false
      }
    ]
  }
}
